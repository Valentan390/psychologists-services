import styled from "styled-components";

export const StyledNoFavoritesWrapper = styled.div`
  margin-left: auto;
  margin-right: auto;
  padding: 33px;
  width: 738px;
  border-radius: 30px;
  border: 1px solid var(--mintGreen);
  box-shadow: 0 0 28px 8px rgba(29, 238, 88, 0.3);
  background: rgba(197, 242, 210, 0.3);
`;

export const StyledNoFavoritesFoto = styled.img`
  width: 128px;
  margin-bottom: 40px;
  margin-left: auto;
  margin-right: auto;
`;

export const StyledNoFavoritesText = styled.p`
  color: var(--deepAnthracite);
  font-size: 40px;
  font-weight: var(--medium);
  line-height: 1.2;
  letter-spacing: -0.8px;
  margin-bottom: 20px;
`;
